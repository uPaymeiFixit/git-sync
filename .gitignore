# Swift Package Manager / Xcode
.build/
.swiftpm/
Package.resolved
*.xcodeproj/
DerivedData/

# Packaged release archives (uploaded to GitHub Releases, not committed).
# appcast.xml IS committed — it's the Sparkle update feed.
GitSync-v*.zip

# Local-only env file (kept as a guard; not used by the app)
.envrc
